# Fraud Detection MLOps Pipeline — pinned dependencies
# Verified to install and run together on Python 3.13.
# Only the packages we import directly are listed here (kept short on purpose
# so every line is explainable); pip pulls their sub-dependencies automatically.

mlflow==2.22.5          # experiment tracking + model registry (NOT mlflow serving)
scikit-learn==1.9.0     # the fraud classifier (LogisticRegression / RandomForest)
pandas==2.3.3           # tabular data handling (capped <3 by mlflow)
numpy==2.5.0            # numerics (used directly for the drift metric)
Flask==3.1.3            # the /predict REST API that wraps the model
python-dotenv==1.2.2    # load the API key from a local .env file
requests==2.34.2        # test client + retrain script talking to the API
